# Maker（実装役）状態管理ファイル

このファイルはMaker（夜間の自動ブラッシュアップ・クラウドルーティン、実装役）が毎回
参照・更新する。同じリポジトリ直下の `STATE.md` はChecker（検証役ルーティン）専用の
状態ファイルであり、`STATE.md` 側に明記されている通り「Maker役はこのファイルを編集
しません」というルールがあるため、Maker側の記録は本ファイルに分離した。
（2026-08-05: MakerとCheckerが同時刻にそれぞれ `STATE.md` を新規作成し衝突したため、
この分離を行った。）

## 引用済み受賞作品（重複禁止・毎回このリストを確認してから選定すること）

過去のコミットメッセージ（`git log site-brushup --grep="参考作品"`）から集計した実績:

1. 'kin（Awwwards/FWA/CSSDA同時受賞・Developer Award）
2. Montfort（Awwwards/FWA/CSSDA同時Site of the Day）
3. Motto（FWA Site of the Day）
4. Naiara Odriozola（CSS Design Awards）
5. Longbow（Awwwards SOTD）
6. PP Neue Montreal（Awwwards SOTD）
7. Mees Verberne（CSSDA/Awwwards/FWA受賞）
8. RISK（Awwwards SOTD）
9. Made With Gsap（Awwwards SOTD）
10. CoffeeTech（Awwwards SOTD）
11. Glitch&Grit（Awwwards SOTD）
12. Lama Lama（Awwwards SOTD）
13. Dragonfly Redux（Awwwards SOTD）
14. House of Honey（Awwwards SOTD）
15. IZANAMI（Awwwards SOTD）
16. Hiroto Sato（Awwwards SOTD）
17. NORMAL IS BORING（Awwwards SOTD）
18. TRIONN（Awwwards/FWA SOTD）
19. MONOLOG（Awwwards SOTD）
20. Spotify Wrapped Party（Awwwards SOTD）
21. Lacoste Polo Factory（Awwwards SOTD）
22. Partizan（Awwwards SOTD）
23. Hildén & Kaira（Awwwards SOTD）
24. Obys Experiment Space（Awwwards SOTD）
25. Der Baukasten（Awwwards/FWA SOTD）
26. Bucks Sauce（Awwwards SOTD）
27. Crazy About Eggs（Awwwards SOTD）
28. Artem Shcherbakov Portfolio（Awwwards SOTD）
29. Hearst Exhibit 2026（OSMOS PRO制作、Awwwards Site of the Day 2026-08-02、Developer Award）
30. Noomo Showcase（Noomo Agency制作、Awwwards Site of the Day 2026-08-01、総合スコア7.34）
31. Lacoste Ace Breaker（Merci-Michel制作、Awwwards Site of the Day 2026-08-03、Developer Award、総合スコア7.46）
32. 2xA Studio（2xA制作、Awwwards Site of the Day 2026-07-31、Developer Award、総合スコア7.22）
33. The Triadic Ballet AI（isaWabi / Isabelle Cuisset制作、CSS Design Awards Website of the Day 2026-08-04）
34. Motiondeep（John Jattoh制作、CSS Design Awards Website of the Day 2026-08-05受賞）
35. Serotoninn（BL/S® / Blacklead Studio制作、Awwwards Site of the Day 2026-08-05受賞、Developer Award同時受賞、総合スコア7.37）
36. Uncommon Studio（オーストラリアのデザインスタジオUncommon自身のポートフォリオサイト、Awwwards Site of the Day + Developer Award、FWA Site of the Day、CSS Design Awards Special Kudosの3団体・4冠受賞）
37. CIAO ENERGY - LAUNCH WEBSITE（Skaald制作、エナジードリンクブランドCiao Energyの新フレーバー発売記念サイト、Awwwards Site of the Day + Developer Award、2026-07-30受賞）
38. Spectral Field（Rob FWA制作、CSS Design Awards Website of the Day受賞。アップロードした音源をFFT解析し、曲ごとに一点物の線画アートワークをブラウザ上で生成する体験）
39. Seunghyuk Kim Interactive Portfolio（韓国のグラフィックデザイナーSeunghyuk Kim制作、CSS Design Awards Site of the Day受賞（2026-08-01）。React Three Fiber/WebGL/GSAPで「点・線・面」という一貫した視覚言語を軸に、サウンドと滑らかなトランジションを組み合わせたインタラクティブポートフォリオ。関連するCodrops記事「Letting the Creative Process Shape a WebGL Portfolio」で、線の前後端に非対称なぼかしを与えて方向性（動きの向き）を持たせる手法が紹介されている）
40. Bogdan Kolomiyets – Portfolio（ウクライナ・オデッサ在住の独立系デザイナー/デベロッパーBogdan Kolomiyets本人の制作。Awwwards Honorable Mention、および2026-08-03にCSS Design Awards（csswinner.com掲載分）でSite of the Dayを受賞。GSAP/JavaScript/Figmaで構築され、ローディングアニメーション・ページ遷移・404ページ・スクロール演出まで一貫したモーション言語で仕上げられている。カラーパレットは温かみのあるオフホワイト(#EAE6DD)と赤みの強いオレンジ(#f05235)の2色構成。本人は「賞を獲ることよりクライアントに専門性を伝え信頼される見た目にすること」を目指すと公言している）
41. Alethia（++hellohello / Pablo Picart / SEB® / Ismael Martínez制作。ブロックチェーンを活用した環境インテリジェンス・プラットフォームAlethiaのサイトで、リアルタイムの環境データ（GHG排出量等の大規模農業モニタリング）を「明快で信頼できるデジタル体験」に変換することを目指した設計。The FWA Site of the Dayおよび2026-08-05にAwwwards Site of the Day（総合スコア7.34）を受賞）
42. **IFF 2025 Sustainability Report（International Flavors & Fragrances）**（Digital Silk制作。国際香料・フレーバー大手IFFの2025年サステナビリティレポートを、単なるPDF/静的レポートではなくスクロールで読み進める「アンフォールディングする物語」として作り直したマイクロサイト。DesignRush Design Awards 2026年7月度「Best Website Design」受賞）← 今回追加

次回以降は必ずこのリストに無い作品を選ぶこと。

## 既知の問題・回避策（Maker視点）

- **WebFetch がこの実行環境で awwwards.com 等の外部サイトに対しほぼ確実に403を返す**。過去の全セッションで再現。回避策: WebSearchのスニペット（Communication Arts、Codrops、Muzli等の紹介記事や検索結果の要約）から評価点を分析する。2026-08-05時点でも `https://www.awwwards.com/sites/hearst-exhibit-2026` はWebFetchで403だった。
- **`npm run build`（Turbopackデフォルト）がGoogle Fontsの取得に失敗して落ちることが過去にあった**（環境のネットワーク状況に依存、ベースコミットでも再現する場合はサンドボックス固有の既知問題としてそのままpushしてよい）。回避策として `next build --webpack` を使った実績もある。2026-08-05時点では `npm run build`（Turbopack）は問題なく成功した。
- `npm install` は毎回のセッション開始時に必要（`node_modules` はクリーンな環境ではコミットされていない）。
- `npm run lint` は本ルーティンの対象外の既存ファイル（`scramble-text.tsx`, `sound-toggle.tsx`, `video-modal.tsx` の `react-hooks/set-state-in-effect`、計4件）で既知のエラーが出るが、Checker側の`STATE.md`でも確認済みの通りビルド自体はブロックしない既存の技術的負債。今回変更したファイルには影響しない。
- **`main` ブランチと `site-brushup` ブランチは無関係な履歴に分岐している**（共通祖先が無い状態）。Checkerの`STATE.md`でも同様の指摘あり。Maker/Checkerどちらもこの分岐自体の解消は自分のスコープ外として扱っている。
- **並行実行時の衝突に注意**: 2026-08-05、MakerとCheckerがほぼ同時刻に`STATE.md`をそれぞれ新規作成しpushし、push時にconflictが発生した。今後は本ファイル（`MAKER_STATE.md`）のみをMakerが編集することで回避する。

## 直近の実行ログ

- **2026-08-06（42回目）**: DesignRush Design Awards 2026年7月度「Best Website Design」受賞作「IFF 2025 Sustainability Report」（Digital Silk制作。国際香料・フレーバー大手International Flavors & Fragrances（IFF）の2025年サステナビリティレポートを、静的なPDF/レポートとしてではなく、スクロールするごとに各セクションが物語のように開いていく「スクロールテリング」体験として作り直したマイクロサイト）を参考に分析。**注記: 今回はAwwwards/CSSDA以外の情報源（DesignRush Design Awards）を選定。WebFetch（designrush.com/best-designs/websites/international-flavors-and-fragrances-website-design、news.designrush.com/designrush-july-2026-design-award-winners）はいずれも403で直接確認できず、WebSearchのスニペット（DesignRush公式ニュース記事、Digital Journal/Yahoo Finance/Globe and Mail等に配信されたプレスリリースの要約）から評価点を分析した**。5つの評価ポイント: ①各セクションが「物語のように開いていく（unfolding narrative）」スクロールテリング構成——ESGレポートという本来無機質な題材を、スクロール操作そのものに意味を持たせて読ませる設計、②常設のトップバーがページ内の「名前付きセクション」に対応し、読者が長いページのどこにいても常に自分の位置を把握できるナビゲーション配慮、③放射状のサプライチェーン図やベントボックス（bento-box）レイアウトで密度の高いESG数値データを視覚的に読みやすくする情報設計、④密なデータを装飾のための演出にせず「わかりやすさ」の道具として使う誠実さ、⑤大手代理店Digital Silkの実装力を示すDesignRush 2026年7月度Best Website Design受賞という裏付け。今日ポケのオレンジ/黒/白3色ルール・実写ブランド素材中心の方針に対し、③のような放射状データ図やベントボックスは既存の実績カウントアップ演出（前回41回目で検証リングを追加済み）と役割が重複するため見送り。今回は②の本質——「常設のトップバーに“今どのセクションにいるか”という名前を対応させ、読者が常に自分の位置を把握できるようにする」という考え方——を、既存のLenis連動スクロール進捗バー（`scroll-progress.tsx`、前回13回目でBogdan Kolomiyets氏を参考に実装済み）に対して抽出し、セクション名ラベルを追従させる拡張として実装した。
  - `src/components/scroll-progress.tsx`: 既存の`ScrollProgress`に任意の`sections`prop（`{id, label}[]`、既定は`undefined`）を追加。指定しない場合は従来通りバーのみの表示で完全に後方互換。指定した場合のみ、各セクション要素の`getBoundingClientRect().top`をLenisの既存`scroll`イベントハンドラ内で軽量にチェックし（新規の`ScrollTrigger`インスタンスは追加していない）、現在ビューポート上部付近にあるセクションのラベルを、バーの先端（進捗位置と同じ`left`）に追従する小さな黒いピル型タグとして表示する。タグは進捗が0%付近・100%付近（ページ最上部/最下部、ヘッダーと重なりうる位置）では`opacity: 0`にフェードアウトし、既存のヘッダーやフッターと視覚的に衝突しないよう配慮した。
  - `src/app/layout.tsx` / `src/app/page.tsx`: `ScrollProgress`は従来`layout.tsx`側で無引数のまま常設していたが、セクション名（`acts`配列）は`page.tsx`側にしか存在しないため、`layout.tsx`から`<ScrollProgress />`を削除し、`page.tsx`側で既存の`acts`（`ActIndex`に渡しているものと同一の配列、tone等の余剰プロパティはそのまま構造的に許容される）を渡す`<ScrollProgress sections={acts} />`として`ActIndex`の直前に配置し直した。本サイトはルートが`/`の1つのみ（`src/app`配下に他のpage/route無し）であることを確認済みで、`position: fixed`のためDOM上のマウント位置を変えても表示上の挙動（最上部固定・z-40）は不変。ナビ項目のラベル・href・`acts`のtone等、既存の事実情報・データは一切変更していない。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な4ファイル・計4件の`react-hooks/set-state-in-effect`：intro-loader.tsx/scramble-text.tsx/sound-toggle.tsx/video-modal.tsx）のみで、変更ファイル（scroll-progress.tsx, layout.tsx, page.tsx）にlintエラーなし。この実行環境ではPlaywrightがプロジェクトの依存関係としては未インストールだが、グローバルにインストール済みの`playwright`パッケージ（`/opt/pw-browsers/chromium`）を利用して`npm run dev`起動後に実際にChromiumで開き、デスクトップ幅（1600px）・モバイル幅（390px）の両方でスクロールしながらスクリーンショットを確認。バー先端の黒いタグがスクロールに追従して「プロフィール」→「実績」とセクション名を正しく切り替え、ヘッダーと重ならず、モバイルでも崩れずに表示されることを目視確認済み。
  - **判定: (Checker確認待ち)**
- **2026-08-06（41回目）**: The FWA Site of the Day / Awwwards Site of the Day「Alethia」（++hellohello / Pablo Picart / SEB® / Ismael Martínez制作。ブロックチェーンを活用した環境インテリジェンス・プラットフォームAlethiaのマーケティングサイトで、リアルタイムの環境データ（GHG排出量等の大規模農業モニタリング）を「明快で信頼できるデジタル体験」へ変換することを目標に、コンテンツ構造・ビジュアル言語・インタラクションを1つの系として設計。2026-08-05 Awwwards SOTD受賞、総合スコア7.34）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/alethia、landing.love/sites/alethia/のいずれも）は403で直接確認できず、WebSearchのスニペット（Awwwards掲載情報の要約、landing.loveのケーススタディ紹介文、alethia.earth本体のサービス概要）から評価点を分析した**。5つの評価ポイント: ①「リアルタイム環境データ」という信頼性が命の題材を、コンテンツ構造・ビジュアル言語・インタラクションを別々の演出の寄せ集めではなく1つの一貫したシステムとして設計することで「明快で信頼できる」体験に変換している統合設計、②ダークグリーン×ライトグリーンという抑制の効いたアースカラー2色構成で、複雑な情報でもスキャンしやすい状態を保つ配色規律、③GHG排出量のような無機質な数値データを、誠実で落ち着いたタイポグラフィとモーションで「trustworthy（信頼できる）」ものとして提示する姿勢、④Framerベースのセクションフローとサブトルなモーションによる、派手さより滑らかさを優先した仕上げ、⑤The FWAとAwwwardsという2団体からSite of the Dayを受賞している総合力。今日ポケのオレンジ/黒/白3色ルール・実写ブランド素材中心の方針に対し、②のアースカラーそのものはブランドカラー規定と衝突するため見送り、④のFramer由来の演出は既存のGSAP/Lenis構成で既に多数実装済みのため重複を避けた。今回は①③の本質——「生の数字を提示するだけで終わらせず、その数字が“確かに検証された”と伝わる一貫した視覚・インタラクションの仕組みを添える」という考え方——を、既存の実績セクションの数字（`StatSpotlight`のチャンネル登録者数・総再生回数等のカウントアップ演出）に対して抽出し実装した。
  - `src/components/stat-counter.tsx`: 既存のカウントアップ用`ScrollTrigger`＋`gsap.to`の`onUpdate`内に、進捗（0〜1）を親へ通知する任意コールバック`onProgress`を追加しただけ。デフォルトは`undefined`のため既存呼び出し側の挙動・シグネチャは完全に後方互換。`prefers-reduced-motion`時は即座に`onProgress?.(1)`を呼び、既存の「即時確定表示」という体験を崩さない。
  - `src/components/stat-spotlight.tsx`: 各数字の下のラベル行に、数字のカウントアップと同期して円弧が描かれ、カウント完了と同時にチェックマークがフェードインする小さなSVGリング（幅18px、lg:20px）を追加。円弧の`strokeDashoffset`とチェックマークの`opacity`は`onProgress`コールバックからrefのDOM直接操作で更新しており、`growth-timeline.tsx`等の既存コンポーネントが採用している「Reactの再レンダリングを介さずrefでDOMを直接更新する」パターンを踏襲した。新しい文言・数値は一切追加していない（純粋に既存の数字を装飾する視覚要素のみ）。既存の`StatSpotlight`のデスクトップ限定ピン留め演出（`gsap.matchMedia`）には手を加えていない。
  - `npm install` → `npm run build`（Turbopack）: 成功。`npm run lint`: 既知の問題（intro-loader.tsx/scramble-text.tsx/sound-toggle.tsx/video-modal.tsxの`react-hooks/set-state-in-effect`計4件）のみで失敗、変更ファイル(stat-counter.tsx, stat-spotlight.tsx)にlintエラーなし。Playwright（`/opt/pw-browsers/chromium`）でdevサーバーを起動し実際にブラウザで実績セクションまでスクロールして確認、リングがカウントアップと同期して描かれチェックマークが表示されることを目視確認済み
  - **判定: (Checker確認待ち)**
- **2026-08-06（40回目）**: Awwwards Honorable Mention / CSS Design Awards Site of the Day（csswinner.com掲載分、2026-08-03受賞）「Bogdan Kolomiyets – Portfolio」（ウクライナ・オデッサ在住の独立系デザイナー/デベロッパー本人のポートフォリオサイト）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/bogdan-kolomiyets-portfolio、bogdankolomiyets.com、cssdesignawards.com のいずれも）は403で直接確認できず、WebSearchのスニペット（Awwwards掲載情報の要約、csswinner.comの受賞記録、本人サイトのabout/紹介文の要約）から評価点を分析した**。5つの評価ポイント: ①企業やチームでなく実質ひとりのデザイナー/デベロッパーによる自主制作でありながら、Awwwards Honorable MentionとCSS Design Awards Site of the Dayという主要2団体から評価を得ている完成度、②本人が「賞を獲ることよりクライアントに専門性を伝え信頼される見た目にすること」を目指すと公言している通り、派手さのための演出ではなく機能に紐づいた節度あるアニメーション設計思想、③温かみのあるオフホワイト(#EAE6DD)と赤みの強いオレンジ(#f05235)という抑制の効いた2色構成のカラーパレット、④ローディングアニメーション・ページ遷移・404ページ・スクロール演出まで、サイトの「あらゆる小さな接点」に同じモーション言語を行き渡らせている仕上げの徹底、⑤写真・動画素材をレイアウトに違和感なく統合しているビジュアル面の完成度。今日ポケの実写ブランド素材中心の方針・単一ページ構成に対し、①⑤のような制作体制そのものや素材統合手法は参考にできても直接実装できる技術要素ではなく、③の配色は既存のオレンジ/黒/白ルールと既に近い方向性のため新規実装の対象としては見送った。今回は②④の本質——「派手な単発演出ではなく、ページのどこにいても同じ抑制されたモーション言語で常時フィードバックする」という考え方——を、これまで未着手だった「今どのくらいスクロールしたか」という最も基本的な接点に対して実装した。
  - `src/lib/lenis.ts`: 新規関数`onLenisReady(cb)`を追加。`SmoothScroll`の`useGSAP`がマウント後にLenisインスタンスを生成するため、生成前に呼ばれても後から一度だけ`cb`を呼べるよう、`setLenis`側で保留中コールバックを解決するキューを追加した。既存の`setLenis`/`scrollToHash`の外部シグネチャ・挙動は無変更。
  - `src/components/scroll-progress.tsx`（新規）: `onLenisReady`経由でLenisインスタンスを取得し、`lenis.on("scroll", ...)`を購読して`progress`（0〜1）を`ref`経由で直接バーの`scaleX`に反映する薄いコンポーネント。`smooth-scroll.tsx`側の`lenis.on('scroll', ScrollTrigger.update)`の連携パターンをそのまま踏襲しつつ、独立した購読として追加したため既存連携への変更は一切ない。見た目は最上部固定の高さ3pxのバーで、背景は`bg-black/5`のトラック＋`bg-brand`（ブランドオレンジ）のインジケータのみといった最小限の実装。`z-40`とすることで、`FullscreenMenu`(z-50)・`VideoModal`(z-60)・`NavTransition`(z-65)・カスタムカーソル(z-998/999)・`IntroLoader`(z-9999)の下に位置し、それらのオーバーレイ表示中は自然に隠れる。スクロール位置をそのまま反映するだけで独自のアニメーションループを持たないため、`prefers-reduced-motion`の追加ガードは不要と判断した。
  - `src/app/layout.tsx`: `<ScrollProgress />`を`<GrainOverlay />`の直後・`<NavTransition />`の直前に追加した1行のみ。既存の`IntroLoader`/`CustomCursor`/`GrainOverlay`/`NavTransition`/`SmoothScroll`の並び・挙動は無変更。
  - `src/app/page.tsx`: 変更なし。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な4ファイル・計4件の`react-hooks/set-state-in-effect`：intro-loader.tsx/scramble-text.tsx/sound-toggle.tsx/video-modal.tsx）のみで、変更ファイル（lenis.ts, scroll-progress.tsx, layout.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、「非公式のファンサイト」を含む既存文言がSSRで期待通り出力されていることを確認）。Playwrightがこの環境に未インストールのため実ブラウザでの目視確認は今回省略（変更はLenisの既存`scroll`イベントを購読してCSS transformを直接更新するだけの低リスクな追加と判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-06（39回目）**: CSS Design Awards Site of the Day「Seunghyuk Kim Interactive Portfolio」（韓国のグラフィックデザイナーSeunghyuk Kim制作、React Three Fiber/Three.js/GSAPで「点・線・面（points, lines, planes）」を通貫する視覚言語を組み立て、サウンドとシームレスなトランジションを重ねた個人ポートフォリオ。CSS Design Awards Site of the Day受賞（2026-08-01））を参考に分析。**注記: 今回もWebFetch（awwwards.com、cssdesignawards.com、orpetron.com、tympanus.net/codropsのいずれも）は403で直接確認できず、WebSearchのスニペット（CSS Winner掲載情報、Orpetronのレビュー要約、および制作者本人と思われる関係者が寄稿したCodrops記事「Letting the Creative Process Shape a WebGL Portfolio」の要約）から評価点を分析した**。5つの評価ポイント: ①「点・線・面」という単一の視覚語彙をサイト全体に一貫して通す構成力（一発ネタの演出の寄せ集めではなく、1つの造形原理からすべてのセクションが導かれる）、②線の前後端に非対称なぼかし（先端はくっきり・後端はぼける）を与えることで、静止した1本の線にも「動きの向き」を感じさせる仕上げの誠実さ、③画面そのものを「スクリーン」ではなく「面（プレーン）」として扱い、about等のコンテンツ表示すらSVGパスのモーフィング（形状変形）で同じ面の上に描き出す一貫性、④WebGL/サウンド/トランジションを組み合わせ視覚と聴覚の両方で「途切れない一続きの体験」を作る統合設計、⑤CSSDA Site of the Day受賞という完成度の裏付け。今日ポケのオレンジ/黒/白3色ルール・実写ブランド素材中心の方針に対し、①③④のような本格的なThree.js/WebGLによる面の変形表現は規模・工数・素材面（実写素材中心の方針）で不釣り合いなため見送り。今回は②の本質——「静止していても、先端はくっきり・後端はぼけるという非対称な仕上げだけで“動きの向き”が伝わる」という考え方——を、既存のカスタムカーソル（`custom-cursor.tsx`）に抽出し、コード・アセットを直接コピーせず独自実装した。
  - `src/components/custom-cursor.tsx`: カーソルの ring/dot/label 追従とは別に、新規の `trailRef` 要素（幅5・高さ3pxの白いカプセル、`mix-blend-difference` で既存のring/dotと視覚言語を統一）を追加。`mousemove` イベントごとに直前の座標との差分から移動速度・向きを計算し、その向きへ `rotation`（進行方向の逆＝後方）で回転、速度に応じて `scaleX`（後方への伸び）と `opacity` を `gsap.set` で直接更新する薄いロジックのみを実装。要素自体には `blur-[2.5px]` の一様なぼかしと、先端(cursorに最も近い側)が不透明・後端が透明になる `mask-image: linear-gradient(to right, black, transparent)` を組み合わせることで、「先端はくっきり・後端はぼける」非対称な軌跡を表現した（Seunghyuk Kim版はThree.jsのシェーダーで実現していると推測されるが、こちらはCSS transform + mask + blurのみの完全に独自実装）。カーソルが止まる・画面外に出ると自然にopacity 0へ収束/フェードし、常時表示の演出にはならない（既存の`active`/`prefers-reduced-motion`/`pointer: coarse`ガードもそのまま維持）。既存のring/dot/labelの追従ロジック・カーソルインデックスプレビュー等は無変更。
  - `src/app/page.tsx`: 変更なし（`CustomCursor`は`src/app/layout.tsx`から従来通り呼び出されるのみ）。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な4ファイル・計4件の`react-hooks/set-state-in-effect`：intro-loader.tsx/scramble-text.tsx/sound-toggle.tsx/video-modal.tsx）のみで、変更ファイル（custom-cursor.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、「非公式ファンサイト」を含む既存文言がSSRで期待通り出力されていることを確認）。Playwrightがこの環境に未インストールのため実ブラウザでの目視確認は今回省略（変更は既存の`gsap.quickTo`/`gsap.set`パターンを踏襲した純CSS transform + mask演出のみで、`prefers-reduced-motion`/`pointer: coarse`時は従来通り丸ごとスキップされるため低リスクな追加と判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-06（38回目）**: CSS Design Awards Website of the Day「Spectral Field」（Rob FWA制作。任意の音源をアップロードすると、ブラウザ内で完結するFFT 2048の解析だけでその曲固有の一点物の線画アートワークを描き出し、PNG/SVGとして書き出せるジェネレーティブ・オーディオビジュアル作品）を参考に分析。**注記: 今回もWebFetch（cssdesignawards.com/sites/spectral-field/49730/）は403で直接確認できず、WebSearchのスニペット（CSS Design Awards掲載情報、robfwa.com自身の紹介文の要約）から評価点を分析した**。5つの評価ポイント: ①音楽そのものを「聴くもの」で終わらせず「一点物の視覚的アートワーク」として描き出す、音→視覚への翻訳という発想の飛躍、②アップロードした音源ファイルが一切サーバーに送信されずブラウザ内（FFT 2048）だけで完結するプライバシー配慮の実装、③周波数・振幅という音の物理特性を、線の揺らぎという視覚言語へ具体的に対応づける変換設計の誠実さ（見た目が「それっぽい」だけでなく実際の音のデータに基づく）、④体験を一過性で終わらせず、生成物をPNG/SVGとして「持ち帰れる成果物」にする配慮、⑤Rob FWA自身の実装によるCSSDA WOTD受賞という完成度。今日ポケのオレンジ/黒/白3色ルール・実写ブランド素材中心の方針に対し、①②のような本格的な音源アップロード＋FFT解析体験そのものは本サイトのスコープ（ファンサイト）を超え規模・工数面で不釣り合いなため見送り。今回は③の本質——「実際に鳴っている音の物理特性（周波数）を、見た目にも同じ根拠で対応させて視覚化する」という誠実さ——を、既存のタップ音基盤（`playPop`のピッチ倍率スイープ）に対して抽出し、独自の軽量な演出として実装した。
  - `src/lib/sound.ts`: `playPop()`が実際に鳴らす周波数スイープ（880Hz→220Hzを`pitch`倍したもの）の定数を`SWEEP_START_HZ`/`SWEEP_END_HZ`に名前付けして明示化。新規関数`popFrequencyProfile(pitch, steps)`を追加し、同じスイープを指定ステップ数でサンプリングして0〜1に正規化した高さの配列を返すようにした（架空の乱数ではなく、実際に鳴る音のパラメータをそのまま流用）。
  - `src/components/spark-tap.tsx`: `SparkTap`のタップ演出に、既存の放射状スパークに加えて新規`spectrumBurst`を追加。`popFrequencyProfile(tone)`から得た高さで6本の縦バー（簡易スペクトラムアナライザー風、色は既存のブランドオレンジ）をタップ位置から生やし、GSAPで一瞬だけ伸び上がって消える一回性の演出にした（Spectral Fieldの「毎回描いて消える」性質を踏襲）。サウンドがオンの時（`isSoundEnabled()`）のみ表示し、「鳴っていない音を視覚化する」矛盾を避けた。既存の放射状スパークやクリック/キーボード操作、`playPop(tone)`の呼び出し自体は無変更。
  - `src/app/page.tsx`: 変更なし。`achievements`配列・`achievementTones`・各`SparkTap`の呼び出し方はすべて既存のまま（`tone`prop経由で自動的にスペクトラムバーの高さへ反映される）。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な4ファイル・計4件の`react-hooks/set-state-in-effect`：intro-loader.tsx/scramble-text.tsx/sound-toggle.tsx/video-modal.tsx）のみで、変更ファイル（sound.ts, spark-tap.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、「実績・出演」を含む既存文言がSSRで期待通り出力されていることを確認）。Playwrightがこの環境に未インストールのため実ブラウザでの目視・聴覚確認は今回省略（変更は既存の`gsap.fromTo`パターンを踏襲したDOM生成のみで、`isSoundEnabled()`がfalseの既定状態では見た目の変化が一切無い低リスクな追加と判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-06（37回目）**: Awwwards Site of the Day + Developer Award「CIAO ENERGY - LAUNCH WEBSITE」（フランスのクリエイティブスタジオSkaald制作。エナジードリンクブランドCiao Energyの新フレーバー発売を記念したWebflow+Three.jsの没入型サイト。2026-07-30 SOTD受賞、Developer Award同時受賞）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/ciao-energy-launch-website、mesh3d.gallery等）はいずれも403で直接確認できず、WebSearchのスニペット（Awwwards掲載情報、Three.jsショーケースサイトの紹介文の要約）から評価点を分析した**。5つの評価ポイント: ①製品そのもの（缶）をThree.js/WebGLで描画し、写真ではなく「触れるインタラクティブな3Dオブジェクト」としてヒーローに据える構成、②その3Dオブジェクトへの操作にサウンドデザインを統合し、視覚だけでなく聴覚でもブランドの「エナジー」感を伝える演出、③装飾を削ぎ落としたミニマルな配色で3Dオブジェクトとモーションに視線の重みを集中させる抑制、④GSAPによる発売ナラティブの滑らかな演出choreography、⑤Developer Award同時受賞が示す、Webflowという制作基盤の上にThree.js/WebGLを破綻なく重ねる実装力の高さ。今日ポケのオレンジ/黒/白3色ルールと軽量CSS/GSAP構成に対し、①⑤のような本格的なThree.js/WebGL3Dオブジェクトは規模・工数・素材面（実写ブランド素材中心の方針）で不釣り合いなため見送り。今回は②の本質——「インタラクションそのものに固有の音を持たせ、触れるたびに製品/ブランドらしい聴覚的アイデンティティが返ってくる」という考え方——を抽出し、既存のタップ音基盤（`src/lib/sound.ts`の`playPop`、`SoundToggle`でオプトイン済み）を拡張する形で独自実装した。
  - `src/lib/sound.ts`: `playPop()`に任意の`pitch`引数（既定値1、後方互換）を追加。指定した倍率を発振周波数（880Hz→220Hzのスイープ）に掛けるだけの薄い変更で、既存の呼び出し（引数なし）の音は完全に不変。
  - `src/components/spark-tap.tsx`: `SparkTap`に任意の`tone`prop（既定値1、`playPop`へそのまま橋渡し）を追加。指定しなければ従来通りの単一音のまま。
  - `src/app/page.tsx`: 「実績・出演」セクションの`achievements`配列（6件、既存のlabel/sub/tone/icon等の事実情報は無変更）に対応する形で、ペンタトニックスケール比率6段階（`[1, 9/8, 5/4, 3/2, 5/3, 2]`、いずれも単純な整数比の純正律でカード同士が不協和にならないよう選定）を定数`achievementTones`として新規追加し、各`SparkTap`にインデックスから決まる固有の`tone`を渡した。タップする実績カードごとに異なる高さの音が鳴るようになる（サウンドは既定オフ、`SoundToggle`でオンにしたユーザーのみに影響する既存の節度をそのまま踏襲）。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な4ファイル・計4件の`react-hooks/set-state-in-effect`：intro-loader.tsx/scramble-text.tsx/sound-toggle.tsx/video-modal.tsx）のみで、変更ファイル（sound.ts, spark-tap.tsx, page.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、実績・出演セクションの既存文言がSSRで期待通り出力されていることを確認）。音は`AudioContext`で都度合成する既存方式をそのまま利用しており新規アセット追加なし。Playwrightがこの環境に未インストールのため実ブラウザでの目視・聴覚確認は今回省略（変更は既存の`playPop`呼び出しに数値を1つ足しただけの低リスクな変更と判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-06（36回目）**: Awwwards Site of the Day「Uncommon Studio」（オーストラリアのデザインスタジオUncommon自身のポートフォリオサイト。Awwwards Site of the Day + Developer Award、FWA Site of the Day、CSS Design Awards Special Kudosという3団体・4冠を受賞）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/uncommon-website、good-design.org、hontran.dev等）はいずれも403で直接確認できず、WebSearchのスニペット（Awwwards掲載情報、Good Design Awards掲載情報、レビュー記事の要約）から評価点を分析した**。5つの評価ポイント: ①「ちょうど良いタイミングで崩れる、確信を持ったグリッド」——均等なグリッドをただ敷くのではなく、要所だけ意図的に崩して視線を誘導する構成美、②GSAPによるセクション間トランジションが「カメラワークのように感じられる」という、単なるフェードではない演出の質、③「作り込まれたクリエイティブスタジオのサイト＝重くて自己満足的」という思い込みを覆す、演出過多でも軽快さを保つパフォーマンス規律、④Awwwards Typography Honors（2025-01）も受賞するタイポグラフィそのものの完成度、⑤Awwwards SOTD+Developer Award・FWA SOTD・CSSDA Special Kudosという3団体4冠の総合力。今日ポケの軽量なCSS/GSAP構成に対し、②のセクション間トランジション演出は既存の`SectionBlend`/`GenerativeTrace`/`ChapterMark`で既に厚めに実装済みのため重複を避け、③（パフォーマンス規律）は新規ビジュアル要素の追加を伴わない監査的な観点のため見送った。今回は①の本質——「均等なグリッドを、意図的に・要所だけ崩すことで視線の重みを作る」という考え方——を、「実績・出演」セクションの6件が横並びの単調な3列グリッドだった箇所に抽出し独自実装した。
  - `src/app/page.tsx`: 「実績・出演」の`achievements`グリッド内、配列の先頭（YouTube Creator Awards 銀の盾、既存データそのまま・並び順も不変）の1枚だけを`sm:col-span-3`でグリッド全幅に広げ、アスペクト比をワイドバナー型（`aspect-[16/9]`〜`sm:aspect-[32/9]`）に、内部レイアウトも縦積みから横並び（アイコン+テキスト）に変更した。他の5枚のカードは既存のレイアウト・アスペクト比・並び順のまま。ラベル・サブテキスト・トーン・アイコン種類等の既存事実情報は一切変更していない（新規データフィールドの追加もなし、`isFlagship`はindexから導出するローカル変数のみ）。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な4ファイル・計4件の`react-hooks/set-state-in-effect`）のみで、変更ファイル（page.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、`sm:col-span-3`を含むflagshipカードのクラスと既存の実績ラベル文言がSSRで期待通り出力されていることを確認）。Playwrightがこの環境のnpmパッケージとして未インストールのため実ブラウザでの目視スクリーンショット確認は今回省略（変更は既存の`SparkTap`/`AchievementIcon`/`SnapReveal`をそのまま再利用したCSS Grid/Flexboxのレイアウト変更のみでリスクは低いと判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-05（29回目）**: Awwwards Site of the Day「Hearst Exhibit 2026」（OSMOS PRO制作、HearstCC / Edoardo Lunardi / Daniel Velasquez / Marjoe Bacus。ELLE・Esquire合同のハリウッド写真展を紹介するギャラリーサイト。2026-08-02 SOTD受賞、Developer Award同時受賞、総合スコア7.21）を参考に分析。**注記: WebFetch（awwwards.com）は今回も403で直接確認できず、WebSearchのスニペットから評価点を分析した**。5つの評価ポイント: ①配色の抑制＝わずか2色（#252525のほぼ黒、#E3E3E3のほぼ白）のミニマルパレットに絞り、写真そのものを主役にする哲学、②WebGLギャラリー＝写真展示に本格的なWebGLトランジションを使用、③GSAPによる滑らかなスクロール演出、④マイクロインタラクション＝ホバー・カーソル演出、⑤タイポグラフィと余白＝クリーンで抑制されたギャラリー的な余白の使い方。今日ポケのオレンジ/黒/白3色ルールに合わない本格WebGL（②相当）は見送り、また①はサムネイルに既に`MonoReveal`（グレースケール→カラー）で近い哲学を実装済みのため重複を避けた。今回は⑤の本質——「意味情報（キャプション）は常時表示せず、興味を持って触れた人にだけ差し出す」という展示物解説プレート的な節度——を抽出し独自実装した。
  - `src/components/gallery-caption.tsx`: 新規作成。`group/gallery`を付けた祖先要素にホバー/フォーカスした時だけ、下端から黒グラデーションのスクリムと共に展示キャプション（アイウォッシュラベル＋一言キャプション、ブランドオレンジの区切り線）が`clip-path`でスライドインする薄いラッパーコンポーネント。GSAP不使用の純CSS transition（既存の`MonoReveal`/`UnderlineLink`と同じ設計方針を踏襲）。
  - `src/app/page.tsx`: 「Meet the Members」の実写マスコットバナー（`hero-mascots.png`）にのみ適用。既存の`BlueprintCorners`の常時表示ラベルは変更せず残し、`GalleryCaption`をホバー時だけ追加で現れる第二層の情報として重ねた。他のセクション（実績カード・動画サムネイル等）には適用せず、節度を保った。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功。型チェック・静的ページ生成まで確認済み。`npm run lint` は上記の既知の問題（本変更と無関係な3ファイル・計4件）のみでビルドはブロックされない。
  - `STATE.md`衝突の経緯: 当初`STATE.md`を新規作成してpushしようとしたところ、Checkerロールが同時刻に別内容の`STATE.md`をpushしており衝突。rebaseの上、Checker側の`STATE.md`をそのまま残し、Maker用の記録は本ファイル（`MAKER_STATE.md`）に分離した。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-05（30回目）**: Awwwards Site of the Day「Noomo Showcase」（Noomo Agency制作。Salesforce・AMD・Coinbaseなど大手ブランド案件を紹介するエージェンシーの実績ショーケースサイト。2026-08-01 SOTD受賞、総合スコア7.34／design 7.29・usability 7.06・creativity 7.79・content 7.51）を参考に分析。**注記: 今回もWebFetch（awwwards.com）は403で直接確認できず、WebSearchのスニペット（Awwwardsの個別インスピレーションページ「Noomo Showcase - Project Hover」「3D hover effect」等の紹介文・スコア情報）から評価点を分析した**。5つの評価ポイント: ①3Dホバープレビュー＝プロジェクト項目にカーソルを合わせると、その場でクリックせずとも中身の質感が伝わる3D/WebGLプレビューが浮かぶ「触れる前に見せる」設計、②WebGL/Three.js/GSAPを使った没入型のモーション演出、③エディトリアルな余白とレイアウトでポートフォリオを見せる構成、④カーソル追従のマイクロインタラクション、⑤4項目（design/usability/creativity/content）で偏りなく高スコアという総合力の高さ。今日ポケのオレンジ/黒/白3色ルールと軽量なCSS/GSAP構成に対し本格的なWebGL/3D（①②相当そのもの）は不釣り合いなため見送り。今回は④の本質——「カーソルに寄り添って“これから見に行く場所”の手がかりを差し出す」というプレビュー体験——を、画像やWebGLを使わずタイポグラフィのみで抽出し独自実装した。
  - `src/components/custom-cursor.tsx`: 既存のカスタムカーソル（リング/ドット/ラベル追従）を拡張。新たに`previewIndex`ステートを追加し、ホバー対象の`data-cursor-index`属性を読み取って、カーソル追従ラベルの上に「区切り線＋セクション番号（例: 03 / 05）」の小さなインデックスチップを重ねて表示するようにした。既存の`labelX/labelY`の位置トラッキング（GSAP `quickTo`）や`mix-blend-difference`の仕組みはそのまま踏襲し、新しい追従ロジックは追加していない。
  - `src/components/underline-link.tsx`: `cursorLabel`・`cursorIndex`の任意propsを追加し、`data-cursor-label`・`data-cursor-index`として`<a>`要素へ橋渡しするだけの薄い変更。デフォルトでは何も指定しなければ従来通り無属性のまま。
  - `src/app/page.tsx`: ヘッダーのグローバルナビ（プロフィール/メンバー/実績/動画/リンク）の`UnderlineLink`にのみ`cursorLabel`（ナビ項目名）と`cursorIndex`（"01 / 05"形式の連番）を付与。他のリンク（YouTubeへ/SNS等）には適用せず、「これから移動するセクションの手がかりを事前に見せる」というナビゲーション文脈に限定して節度を保った。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功。型チェック・静的ページ生成まで確認済み。`npm run lint` は既知の問題（本変更と無関係な3ファイル・計4件の`react-hooks/set-state-in-effect`）のみでビルドはブロックされない。Playwrightがこの環境に未インストールのため実ブラウザでの目視スクリーンショット確認は今回省略した（コード変更は既存の`data-cursor-label`機構を素直に拡張したのみで、リスクは低いと判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-05（32回目）**: Awwwards Site of the Day「2xA Studio」（Amsterdam/Athens拠点のコード駆動デザインスタジオ2xA自身のポートフォリオサイト。2026-07-31 SOTD受賞、Developer Award同時受賞、総合スコア7.22）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/2xa-studio、2xa.studio本体とも）は403で直接確認できず、WebSearchのスニペット（Awwwards掲載記事、2xa.studio/aboutの紹介文の要約）から評価点を分析した**。5つの評価ポイント: ①わずか2色（#0F0F0Fのほぼ黒、#FDFDFDのほぼ白）に絞ったミニマルパレットで、コードそのものが生む造形を主役にする抑制されたデザイン、②"Form follows process"というジェネレーティブ/コンピュテーショナルなデザイン哲学——「静的な成果物ではなく、条件が動きの中に解き放たれたシステム」として作品を捉える姿勢、③Developer Award同時受賞が示す高い実装力、④タイポグラフィ主体で装飾を削ぎ落としたレイアウト、⑤アニメーション・モーションデザインの完成度。今日ポケのオレンジ/黒/白3色ルールに対し、②の哲学そのもの（本格的なジェネレーティブアート/WebGL）は規模・工数の面で不釣り合いなため見送り。今回は②の本質——「同じ生成ルール（システム）を場所ごとに走らせることで、毎回違うが一貫した“構造”が現れる」という考え方——を、既存のセクション間トランジション（`SectionBlend`の色帯）に重ねる形で、Math.random()を使わないseed値だけで決まる決定論的な折れ線（基板配線のような`GenerativeTrace`）として抽出し独自実装した。
  - `src/components/generative-trace.tsx`: 新規作成。`seed`値のみから経路が決まる自前の擬似乱数（mulberry32）で、基板配線のような直角の折れ線パスをSVGで生成するコンポーネント。GSAP `ScrollTrigger`（`scrub`）でセクション境界をスクロールする間だけ`stroke-dashoffset`を使って線が「描かれていく」演出を付け、既存の`smooth-scroll.tsx`のLenis↔ScrollTrigger連携（`lenis.on('scroll', ScrollTrigger.update)`）はそのまま利用（変更なし）。線は`mix-blend-difference`の白で描画するため、下地がどの背景色（`#f5f5f5`/`#000000`/`#d9552e`のいずれ）でも自動的に反転して視認できる。
  - `src/app/page.tsx`: 既存の4箇所の`SectionBlend`（セクション間の色帯）それぞれを`relative`な`div`で包み、異なる`seed`（1〜4）を渡した`GenerativeTrace`を重ねただけ。色帯以外の新しいセクションや文言は追加していない。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な3ファイル・計4件の`react-hooks/set-state-in-effect`）のみで、新規ファイル（generative-trace.tsx）・変更ファイル（page.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、「実績・出演」等の既存文言を含み、新規`GenerativeTrace`のSVG（`viewBox="0 0 100 24"`）が4箇所とも期待通りSSRされていることを確認）。Playwrightでの目視スクリーンショット確認は今回省略（変更は既存パターン踏襲のCSS/SVG演出のみでリスクは低いと判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-05（33回目）**: CSS Design Awards「The Triadic Ballet AI」（isaWabi（Isabelle Cuisset）制作。Bauhausの舞踊家Oskar Schlemmerが1922年に発表した「トライアディック・バレエ」をAI画像/モーションで再解釈したデジタル展示サイト。2026-08-04 Website of the Day受賞）を参考に分析。**注記: 今回もWebFetch（awwwards.com、thetriadicballetai.com本体とも）は403で直接取得できず、WebSearchのスニペット（Awwwardsノミネートページ・作品概要記事の要約）から評価点を分析した**。5つの評価ポイント: ①全3幕それぞれに異なる「舞台色」（第1幕は黄色の緞帳、等）を割り当て、幕が変わるごとに作品のトーン・テンポの転換を色で明示する構成美、②Schlemmerの「人体を円錐・球体などの幾何学形状へ還元する」という原作の思想をAIで再解釈した造形、③GSAPによるスクロール駆動の演出（静的ページを閲覧者のスクロールで制御される体験に変換）、④装飾を削ぎ落としたタイポグラフィ主体のミニマルなレイアウトと場面転換、⑤衣装・音・動き／空間・形・色という「三位一体（トライアド）」の対応関係を意識した設計原則。今日ポケのオレンジ/黒/白3色ルールに対し、AI生成ビジュアルによる幾何学的な人体表現（②相当）は素材面で本サイトの実写ブランド素材（`hero-mascots.png`等）の方針と衝突するため見送り。今回は①の本質——「今どの幕（トーン）にいるかを、色そのもので常に示す」という構造——を、既存の5セクション（プロフィール/メンバー/実績/動画/リンク）にそのまま重ね、各セクションの背景トーン（light/dark/brand）を色分けした常設インデックスとして独自実装した。
  - `src/components/act-index.tsx`: 新規作成。デスクトップ（`lg:`）でのみ表示される、画面右端に固定された縦型の「幕（Act）インデックス」。5つのセクションに対応する5つのドットを表示し、各ドットの色は該当セクションの舞台トーン（light=`neutral-300`、dark=`black`、brand=`brand`オレンジ）で塗り分ける。GSAP `ScrollTrigger`（各セクションの`top center`〜`bottom center`）で現在ビューポート中央にあるセクションを判定し、該当ドットを拡大＋隣にセクション名ラベルを表示。既存の`smooth-scroll.tsx`のLenis↔ScrollTrigger連携は変更せず、既存の`ScrollTrigger.create`の使い方（`chapter-mark.tsx`等）に倣った。ドットは`<a href="#...">`のアンカーとしても機能し、既存の`navItems`と同じセクションへのショートカットにもなる副次効果を持つが、主目的はナビゲーションの重複ではなく「今どの幕にいるか」を常に示す舞台色インデックスとしての演出。
  - `src/app/page.tsx`: 既存の`navItems`と対になる`acts`配列（id/label/tone）を新規追加し、`<ActIndex acts={acts} />`をルート要素直下（`<header>`の直前）に1箇所追加しただけ。tone（light/dark/brand）は既存の`ChapterMark`に渡している`tone`props（`index={3}`のみ`tone="dark"`、`index={5}`のみ`tone="brand"`、他は既定のlight）とセクションごとに完全に一致させており、新しい事実情報・文言の変更は無い。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功。型チェック・静的ページ生成まで確認済み（初回、`readonly`配列を渡す箇所で型エラーが1件出たため`ActIndex`のprops型を`readonly`配列許容に修正し再ビルドして解消。この1回の修正のみでサーキットブレーカーの上限には達していない）。`npm run lint`は既知の問題（本変更と無関係な3ファイル・計4件の`react-hooks/set-state-in-effect`）のみで、新規/変更ファイル（act-index.tsx, page.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、`aria-label="セクション目次"`のnavが期待通りSSRされていることを確認）。Playwrightがこの環境に未インストールのため実ブラウザでの目視スクリーンショット確認は今回も省略（変更は既存の`ScrollTrigger.create`パターンを踏襲したCSS駆動の固定要素のみでリスクは低いと判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-05（31回目）**: Awwwards Site of the Day「Lacoste Ace Breaker」（Merci-Michel制作。Roland Garros開催に合わせたLacosteのブラウザゲーム、ブロック崩し風のテニステーマゲームで本物のチケット・ポロシャツが当たる企画。2026-08-03 SOTD受賞、Developer Award同時受賞、総合スコア7.46）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/lacoste-ace-breaker）は403で直接確認できず、WebSearchのスニペット（Awwwards掲載記事・紹介ブログの評からの要約）から評価点を分析した**。5つの評価ポイント: ①パワーアップ等のアイコノグラフィをLacosteブランドカラー（グリーン/ホワイト/イエロー）で色分けし、本文を読まずとも一目で種類が伝わる高コントラストな「即時可読性」の設計、②ワンバーブ（ブロックを崩す、の一動作）に絞ったゲームメカニクスによる導線のシンプルさ、③本番プレイ前に簡潔なルール・特典説明画面を挟むオンボーディング、④リーダーボード＋実際の景品というリプレイ性を生むゲーミフィケーション、⑤WebGL/Three.jsによる本格的な3D演出。今日ポケのオレンジ/黒/白3色ルールと軽量なCSS構成に対し、本格的なゲーム実装（②③④相当）やWebGL（⑤）は規模・工数の面で不釣り合いなため見送り。今回は①の本質——「本文を読む前に、色分けされたアイコンだけで種類が一目で伝わる」設計——を、実績セクションのカードに抽出し独自実装した。
  - `src/components/achievement-icon.tsx`: 新規作成。実績カード用の丸バッジアイコンコンポーネント。award（トロフィー）/tv/controller/globe/live/cardsの6種類の線画SVGアイコンを用意し、カードの背景トーン（オレンジ/黒）に応じてバッジの配色を反転（オレンジ背景カードには白バッジ+オレンジ文字のアイコン、黒背景カードにはオレンジバッジ+白アイコン）することで、既存のブランド3色のみで高コントラストな色分けを実現。GSAP不使用の純SVG+CSSで既存コンポーネント群と同じ軽量方針を踏襲。
  - `src/app/page.tsx`: 「実績・出演」セクションの`achievements`データ配列に`icon`フィールド（表示用メタデータのみ）を追加し、各`SparkTap`カードの本文テキストの前に`AchievementIcon`を配置。ラベル・サブテキスト・トーン等の既存事実情報は一切変更していない。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）はこの実行環境でGoogle Fonts取得に失敗し既知の問題として再現（`next build --webpack`では正常に成功しTypeScriptチェック・静的ページ生成まで確認済み。これは本変更が原因ではなく既知の環境要因）。`npm run lint` は既知の問題（本変更と無関係な3ファイル・計4件の`react-hooks/set-state-in-effect`）のみで、新規/変更ファイル（achievement-icon.tsx, page.tsx）にlintエラーなし。dev serverを起動しHTML応答（200、実績・出演の文言を含む）を確認。Playwrightがこの環境に未インストールのため実ブラウザでの目視スクリーンショット確認は省略。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-06（35回目）**: Awwwards Site of the Day「Serotoninn」（チェコ拠点BL/S® / Blacklead Studio制作、クリエイティブディレクターSerhii Polyvanyi。グラム/グランジの中間に位置するファッションブランドのEコマースサイト。2026-08-05 SOTD受賞、Developer Award同時受賞、総合スコア7.37）を参考に分析。**注記: 今回もWebFetch（awwwards.com/sites/serotoninn）は403で直接確認できず、WebSearchのスニペット（Behance「BL/S® Studio」、Communication Arts Web Picks、Muzli掲載のケーススタディ記事「Serotoninn Ecommerce Website // Case Study_Vol.1.0」の要約）から評価点を分析した**。5つの評価ポイント: ①赤(#ED3833)と黒(#000000)のわずか2色に絞ったブルータリスト寄りの配色で、シャープな書体と重厚なキャンペーン写真のコントラストのみでブランド世界観を作る抑制、②メニュー自体を「ムードセッター」として設計——メニューを開くとカテゴリごとに専用の動画・イメージが切り替わり、何もクリックしていない段階でそのセクションの空気感が伝わるナビゲーション、③ナビゲーションからコレクションページへの遷移が「断絶ではなく連続」として感じられるトランジション設計、④ナイトライフ/アンダーグラウンドファッション/シネマという一貫した視覚言語でブランドユニバースを統一、⑤GSAP・Swiper.jsによる実装でBehance425 appreciationsを集めた完成度。今日ポケのオレンジ/黒/白3色ルールに対し、②そのもの（カテゴリごとの専用動画を都度用意するメガメニュー）は素材面・工数面で不釣り合いなため見送り。今回は②の本質——「クリックする前に、これから見に行くセクションの空気感（トーン色＋代表カット）だけを差し出す」という先出しの節度——を、ヘッダーナビ自体に抽出し独自実装した。
  - `src/components/nav-mood-preview.tsx`: 新規作成。ヘッダーナビ項目にホバー/フォーカスすると、直下にその項目のトーン色（`ActIndex`と同じlight/dark/brandの3トーン）と代表カット（画像 or 自前SVGアイコン）を乗せた小さなフローティングプレビューパネルが浮かぶコンポーネント。既存の`UnderlineLink`のホバー下線・`data-cursor-label`/`data-cursor-index`（`custom-cursor.tsx`のカーソル追従ラベル）の仕組みはそのまま内包して壊さず、GSAP不使用の純CSS transition（`MonoReveal`/`GalleryCaption`/`FrameScrub`と同じ設計方針）でフェード＋わずかな移動のみを行う。
  - `src/app/page.tsx`: ヘッダーの`<nav>`直書きの`navItems.map`ループを`<NavMoodPreview items={navMoodItems} />`に置き換え。`navMoodItems`は既存の`navItems`（プロフィール/メンバー/実績/動画/リンク、ラベル・href不変）をベースに、トーンは既存`acts`配列のtoneとセクションごとに一致させ、代表カットは新規素材を追加せず**既存の実素材のみ**を再利用（プロフィール＝`public/icon.png`、メンバー＝`public/hero-mascots.png`、動画＝既存の`videos[0].videoId`から生成する`img.youtube.com`のサムネイル、実績＝既存`AchievementIcon`(award)、リンク＝既存`LogoMark`）。キャプション文言は各セクションの内容を要約した新規の一言のみで、メンバー紹介文・実績数値・リンクURL等の既存事実情報は無変更。ナビ本体が不要になった`UnderlineLink`の直接importは削除。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な3ファイル・計4件の`react-hooks/set-state-in-effect`）のみで、新規/変更ファイル（nav-mood-preview.tsx, page.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、5項目分のプレビューキャプション文言がすべてSSRで期待通り出力されていることを確認）。Playwrightがこの環境に未インストールのため実ブラウザでの目視スクリーンショット確認は今回省略（変更は既存パターン踏襲のCSS opacity/transform遷移のみでリスクは低いと判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
- **2026-08-05（34回目）**: CSS Design Awards「Motiondeep」（John Jattoh制作。モーションデザイン/2D・3D/インタラクティブゲーム開発/VFX/UI・UXモーションシステムを横断する本人のポートフォリオサイト。2026-08-05 Website of the Day受賞）を参考に分析。**注記: 今回はWebFetchで cssdesignawards.com・awwwards.com・motiondeep.com・thefwa.com・winners.webbyawards.com のいずれも403で直接確認できず、WebSearchのスニペット（本人の肩書き「Motion Designer | 2D / 3D Specialist | Animator」、姉妹サイトjohnjattoh.com（Awwwards Honorable Mention、タグ「Clean, Minimal, Single page, 3D elements with Microinteractions」）、CSSDAのUI/UX/Innovation評価基準の解説記事）から評価点を分析した**。5つの評価ポイント: ①モーション/2D・3D/ゲーム開発/VFX/UI-UXモーションシステムという畑違いに見える専門分野を1つの人格・1サイトへ統合する構成力、②CSSDA WOTDが要求するUI・UX・Innovation全軸平均8.00超という「見た目だけでなく機能性も同時に高い」バランス、③姉妹サイト(johnjattoh.com)がAwwwards Honorable Mentionで示す通り、モーション専門家でありながら「Clean, Minimal, Single page」という抑制も両立させている点、④「動きを見せる仕事をする人自身のサイトが、その動き自体で実力を証明する」という職能ブランディングの構造（説明文やショーリール動画に頼らずインターフェース自体が実演になる）、⑤ショーリール文化由来の「1枚の静止画ではなく複数の瞬間を予感させて“これは動く”と伝える」提示作法。今日ポケのブランド3色・軽量CSS/GSAP方針に対し、①②のような専門分野統合や本格的なUI/UX刷新は本サイトの本質（ファンサイト）とは無関係なため見送り。今回は⑤の本質——「クリックする前から、これは静止画ではなく動画だと予感させる」提示作法——を、YouTubeが動画ごとに自動生成する3枚のフレーム画像（`1.jpg`/`2.jpg`/`3.jpg`、動画内のおよそ25%/50%/75%地点、既存の`hqdefault.jpg`と同じ`img.youtube.com`ホストで追加素材不要）を使い、サムネイル上でのポインター横位置に応じてクロスフェードする独自実装として抽出した。
  - `src/components/frame-scrub.tsx`: 新規作成。`videoId`のみを受け取り、`hqdefault.jpg`（アイドル状態）と`1.jpg`/`2.jpg`/`3.jpg`の4枚を絶対配置で重ね、`pointermove`のX位置に応じて該当フレームの`opacity`を1にしcrossfadeさせる薄いラッパーコンポーネント。GSAP不使用の純CSS transition（既存の`MonoReveal`/`GalleryCaption`と同じ設計方針を踏襲）。追加のスクロールバー等のUIチープは付けず、既存の再生ボタンオーバーレイと`MonoReveal`のグレースケール演出はそのまま。
  - `src/app/page.tsx`: 「おすすめ動画」セクションの各サムネイル（`<Image src=".../hqdefault.jpg" .../>` 単体）を `<FrameScrub videoId={v.videoId} .../>` に置き換えただけ。動画ID・タイトル・TODO文言等の既存事実情報は変更なし。置き換えにより`next/image`の直接importが不要になったため未使用importとして削除（`ParallaxImage`など他コンポーネント経由の利用には影響なし）。
  - ビルド結果: `npm install` → `npm run build`（Turbopack）で成功（Google Fontsの取得も問題なし、修正リトライなしの1回で成功）。型チェック・静的ページ生成まで確認済み。`npm run lint`は既知の問題（本変更と無関係な3ファイル・計4件の`react-hooks/set-state-in-effect`）のみで、新規/変更ファイル（frame-scrub.tsx, page.tsx）にlintエラーなし。`npm run dev`を起動しHTMLを取得（200応答、`hqdefault.jpg`/`1.jpg`/`2.jpg`/`3.jpg`の4枚とも3動画分（計12枚）がSSRで期待通り出力されていることを確認）。Playwrightでの目視スクリーンショット確認は今回省略（変更は既存パターン踏襲のCSS opacity遷移のみでリスクは低いと判断）。
  - コミットハッシュ: （このコミット自体。`git log -1`参照）
